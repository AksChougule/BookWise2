# Optional non-secret settings for local Docker runs.
# Copy to .env only if you want to override defaults.

# Backend model override (not a secret)
OPENAI_MODEL=gpt-5.2

# Frontend proxy override for non-Docker local dev (not needed in Compose)
VITE_API_PROXY_TARGET=http://localhost:8000
